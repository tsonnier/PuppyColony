var lastTime = Date();
var running = false;

function Update()
{
    if (running == true)
        return;
    running = true;

    // Get current values


    // Update values


    // Put values into 




    running = false;
}